package basic

fun main () {
    /*
    // Lambda Expression with Parameters:
    val sum: (Int, Int) -> Int = { a, b -> a + b }
    println(sum(2, 3)) // Output: 5

    //Lambda Expression with Implicit Parameter:
    val isEven: (Int) -> Boolean = { it % 2 == 0 }
    println(isEven(4)) // Output: true

    //Lambda Expression as Function Argument:
    val numbers = listOf(1, 2, 3, 4, 5)
    val doubled = numbers.map { it * 2 }
    println(doubled) // Output: [2, 4, 6, 8, 10]

    //Lambda Expression with No Parameters:
    val printHello: () -> Unit = { println("Hello") }
    printHello() // Output: Hello

    //Lambda Expression with Nullable Receiver:
    val nullableLength: String?.() -> Int = { this?.length ?: 0 }
    val text: String? = "Hello"
    println(text.nullableLength()) // Output: 5

    // Lambda Expression with Receiver:
    val appendExclamation: StringBuilder.() -> StringBuilder = {
        append("!")
        this // return the modified receiver
    }
    val sb = StringBuilder("Hello")
    sb.appendExclamation()
    println(sb.toString()) // Output: Hello!

    // Higher-Order Function with Lambda Expression:
    fun performOperation(a: Int, b: Int, operation: (Int, Int) -> Int): Int {
        return operation(a, b)
    }
    val sum = performOperation(2, 3) { x, y -> x + y }
    println(sum) // Output: 5
    */
}
